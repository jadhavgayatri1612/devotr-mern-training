# Git Notes

1. git init — Initializes a Git repository

2. git clone — Copies a repository from GitHub

3. git status — Shows changed files

4. git add — Adds files to staging area

5. git commit — Saves changes with a message

6. git push — Uploads code to GitHub